<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.czl">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Czl">
        <activity android:name=".privacy.PrivacyActivity"></activity>
        <activity android:name=".component.Service2Activity" />
        <activity android:name=".component.Service1Activity" />

        <service
            android:name=".component.Service1"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".component.Fragment1Activity" />
        <activity android:name=".component.Activity2" />
        <activity android:name=".component.Activity1" />
        <activity android:name=".RVActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>